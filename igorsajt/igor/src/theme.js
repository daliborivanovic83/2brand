import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#black",
      light: "#white",
    },
    secondary: {
      main: "#black",
    },
    otherColor: {
      main: "#gray",
    },
  },
});
